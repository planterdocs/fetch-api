'use strict'

const findAll = {
  response: {
    200: {
      type: 'array'
    }
  }
}

const findByRegion = {
  response: {
    200: {
      type: 'array'
    }
  }
}

module.exports = { findAll, findByRegion }
